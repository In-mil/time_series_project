name: Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'
          cache-dependency-path: 'requirements.txt'

      - name: Cache Python dependencies
        uses: actions/cache@v4
        id: pip-cache
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Configure Google Cloud & DVC
        env:
          GCP_KEY: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}
        run: |
          set -euo pipefail
          echo "$GCP_KEY" > gcp_creds.json
          gcloud auth activate-service-account --key-file=gcp_creds.json || true
          dvc remote modify gcsremote credentialpath gcp_creds.json || true
          dvc remote default gcsremote || true

      - name: Pull models and artifacts from DVC
        run: |
          set -euo pipefail
          echo "Pulling models and artifacts..."
          dvc pull models/model_ann.keras.dvc || echo "Failed to pull ANN model"
          dvc pull models/model_gru.keras.dvc || echo "Failed to pull GRU model"
          dvc pull models/model_lstm.keras.dvc || echo "Failed to pull LSTM model"
          dvc pull models/model_transformer.keras.dvc || echo "Failed to pull Transformer model"
          dvc pull artifacts/ensemble/ -r gcsremote || echo "Failed to pull ensemble artifacts"

      - name: Run unit tests
        run: |
          pytest tests/ -v --tb=short --cov=service --cov-report=xml --cov-report=term

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

      - name: Generate coverage badge
        run: |
          coverage_percent=$(grep -oP 'line-rate="\K[0-9.]+' coverage.xml | head -1 | awk '{print int($1 * 100)}')
          echo "Coverage: ${coverage_percent}%"
          echo "COVERAGE=${coverage_percent}" >> $GITHUB_ENV

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            htmlcov/
            coverage.xml
            .coverage
